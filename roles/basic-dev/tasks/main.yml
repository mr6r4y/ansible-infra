---

- name: Install packeges for basic-dev
  apt:
    name: "{{ item }}"
    state: present
  with_items: "{{ packages }}"

- name: Prepare /opt/repo folder
  file:
    path: /opt/repo
    state: directory
    group: ansadm
    owner: ansadm
    mode: 0755
